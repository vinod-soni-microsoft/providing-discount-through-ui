# Providing a discount through UI - GPT solution written in python
Providing a discount through UI - GPT solution written in python


## Steps
go to git command prompt
go to scr folder
create .env file
add variable in .env file and add key OPENAI_API_KEY and specify key value (you can create key at openai portal).
create virtual environment
cd src
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
Run the code
uvicorn main:app
uviconre main:app --reload
refresh page


### Instructor

Vinod Soni
